# Incidents and known traps

> One entry per lesson learned in production, per CLAUDE.md's "Known traps" list.
> Newest first. Each entry: what happened, why, what changed as a result.

Empty as of Phase 0 — this file starts filling in from Phase 1 on, as the
traps CLAUDE.md already documents in the abstract (reasoning-model empty
completions, resilience not extending between call sites, similarity
thresholds not judging semantic relevance, ...) are actually hit or
deliberately guarded against in code.
